[alias] 
	st = status
	ci = commit
	co = checkout
	br = branch
[user]
	name = heureux
	email = info@rare.to
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[merge]
	keepBackup = false;
	tool = p4merge
[mergetool "p4merge"]
	cmd = p4merge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
	keepTemporaries = false
	trustExitCode = false
	keepBackup = false
[diff]
	external = p4diff

